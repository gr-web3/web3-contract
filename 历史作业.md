# Web3 & Solidity BootCamp 历史作业

---

## 第三课

### 编写一个 Bank 合约（代码提交到 GitHub）
- 通过 MetaMask 向 `Bank` 合约存款（转账 ETH）
- 在 `Bank` 合约记录每个地址存款金额
- 用数组记录存款金额前 3 名
- 编写 `Bank` 合约 `withdraw()` 方法，实现只有管理员提取出所有的 ETH

> [!TIP]
> **Quest 链接**: [Decert Quest: c43324bc](https://decert.me/quests/c43324bc-0220-4e81-b533-668fa644c1c3)

---

## 第四课

### 编写一个 BigBank 合约（继承自 Bank）
- 仅 `> 0.001 ether`（用 modifier 权限控制）可以存款
- 把管理员转移给 `Admin` 合约，`Admin` 调用 `BigBank` 的 `withdraw()` 方法

> [!TIP]
> **Quest 链接**: [Decert Quest: 063c14be](https://decert.me/quests/063c14be-d3e6-41e0-a243-54e35b1dde58)

---

## 第五课

### 自己编写并部署一个 ERC20 Token
- 编写一个 `TokenBank` 合约，可以将 Token 存入 `TokenBank`：
  - 记录每个用户存入的 Token 数量
  - 管理员可以提取所有的 Token（`withdraw()` 方法）

> [!TIP]
> - **Quest 链接**: [Decert Quest: eeb9f7d8](https://decert.me/quests/eeb9f7d8-6fd0-4c38-b09c-75a29bd53af3)
> - **Challenge 链接**: [Decert Challenge: aa45f136](https://decert.me/challenge/aa45f136-27a3-4bc9-b4f7-15308e1e0daa)

> [!NOTE]
> **思考题**：在 ERC20 转账到合约地址（假设为 `TokenBank`）时，`TokenBank` 合约代码无法执行，`TokenBank` 如何记录用户存了多少 Token？

---

## 第六课

### 6.1 NFT 与 ERC20 交互
- **NFT 合约编写**：
  - 发行一个 ERC721 Token（使用自己的名字）
  - 铸造几个 NFT，在测试网上发行，并在 OpenSea 上查看
- **编写市场合约 NFTMarket**：
  - 支持使用自己发行的 ERC20 Token 来购买 NFT
  - NFT 持有者可上架 NFT（`list()` 方法：设置价格，即需要多少个 ERC20 Token 购买该 NFT）
  - 编写购买 NFT 的方法 `buyNFT(uint tokenId, uint amount)`，转入对应的 ERC20 Token 并获取对应的 NFT
  - 实现 ERC20 扩展 Token 所要求的接收者方法 `tokensReceived`，并在 `tokensReceived` 中实现 NFT 购买功能（注意：扩展的转账需要添加一个额外数据参数）

> [!TIP]
> **Challenge 链接**: [Decert Challenge: 852f5836](https://decert.me/challenge/852f5836-a03d-4483-a7e0-b0f6f8bda01c)

### 6.2 熟悉 ERC721 实现
- 阅读并理解 OpenZeppelin [ERC721.sol 源码](https://github.com/OpenZeppelin/openzeppelin-contracts/blob/master/contracts/token/ERC721/ERC721.sol)
- 理解 `safeTransferFrom` 与合约如何接收 NFT
- 实现 `onERC721Received()` 接收回调函数

### 6.3 部署 ERC20 Token 与 TokenBank
- 编写一个 `TokenBank`，可以将 Token 存入 `TokenBank`：
  - 记录每个用户存入的 Token 数量
  - 管理员/用户可以提取所有的 Token（`withdraw()` 方法）

> [!TIP]
> **参考代码**: [GitHub - TokenBank.sol](https://github.com/lbc-team/Web3-BootCamp-Practice/blob/main/TokenBank_v1/TokenBank.sol)

---

## 第七课

### Foundry 基础与实践
- 完成 Foundry 基础知识测试
- 使用 Foundry 开发、部署并开源 `MyToken` 合约

> [!TIP]
> - **测试链接**: [Decert Quest: 3bca8f1f](https://decert.me/quests/3bca8f1f-df6b-469b-941e-79388ee280c6)
> - **实践链接**: [Decert Quest: 7bd246d8](https://decert.me/quests/7bd246d8-f0c3-45c0-a335-766505afdba9)

---

## 第八课

### Foundry 测试编写
- 为 `Bank` 合约编写测试用例
- 将 `NFTMarket` 合约放入 Foundry 工程并完成测试

> [!TIP]
> - **Bank 测试**: [Decert Quest: b8cde6b2](https://decert.me/quests/b8cde6b2-bad4-4629-b73a-2d0dede4f347)
> - **NFTMarket 测试**: [Decert Challenge: 08973815](https://decert.me/challenge/08973815-3ebe-48d1-915e-7fc67c448763)

---

## 第九课

### 前端与后端集成
- **前端开发**：给 `TokenBank` 添加前端界面：
  - 显示当前 Token 的余额
  - 支持存款到 `TokenBank`
  - 存款后显示用户存款金额，同时支持用户取款
- **后端开发**：使用 `Viem.sh` 监听 `NFTMarket` 的买卖记录：
  - 在 `NFTMarket` 合约的上架（`list`）和买卖函数（`buyNFT` 及 `tokensReceived`）中添加相应事件
  - 后台监听上架和买卖事件，如果链上发生了上架或买卖行为，打印出相应的日志

> [!TIP]
> - **前端 Quest**: [Decert Quest: 56e455b3](https://decert.me/quests/56e455b3-901c-415d-90c0-a20759469cf9)
> - **后端 Quest**: [Decert Quest: b4698649](https://decert.me/quests/b4698649-25b2-45ae-9bb5-23da0c49e491)

---

## 第十课

### 前端接入 AppKit 登录
- 为 `NFTMarket` 项目添加前端，并接入 `AppKit` 进行前端登录
- 接入 `AppKit` 后，支持使用 `WalletConnect` 进行登录

> [!TIP]
> **Quest 链接**: [Decert Quest: a1a9aff6](https://decert.me/quests/a1a9aff6-1788-4254-bc47-405cc529bbd1)

---

## 第十一课

### 多签钱包实践
- 学习并使用 Safe 多签钱包
- 实现一个简单的多签钱包合约，具备以下功能：
  - 多签持有人可以提交交易
  - 其他多签人确认交易（使用交易的方式确认即可）
  - 达到多签门槛后，任何人都可以执行该交易

> [!TIP]
> - **Safe 多签 Quest**: [Decert Quest: 4d4d50ab](https://decert.me/quests/4d4d50ab-84ab-4289-ac67-e3839e078537)
> - **简单多签合约 Quest**: [Decert Quest: f832d7a2](https://decert.me/quests/f832d7a2-2806-4ad9-8560-a27ad8570c6f)

---

## 第十二课

### Permit2 签名授权存款
- 在原有的 `TokenBank` 合约中添加 `depositWithPermit2()` 方法，使用 `Permit2` 进行签名授权转账存款
- 修改 Token 存款前端，让用户可以在前端通过 `Permit2` 的签名进行存款

---

## 第十三课

### 转账记录索引与展示
- **后端**：索引出之前自己发行的 ERC20 Token 转账记录并保存到数据库中，提供一个 RESTful 接口来获取某一个地址 of 转账记录
- **前端**：在用户登录后，从后端接口查询该用户地址 of 转账记录并进行展示

> [!TIP]
> **Quest 链接**: [Decert Quest: ae220513](https://decert.me/quests/ae220513-c0cb-4d9b-873a-caee1d4b358e)

---

## 第十四课

### Meme 铭文铸造工厂合约
- 实现一个工厂合约，在以太坊上使用最小代理（Minimal Proxy / EIP-1167）模拟 Meme 铭文铸造：
  - **方法 1**：`deployInscription(string symbol, uint totalSupply, uint perMint)`
  - **方法 2**：`mintInscription(address tokenAddr)`

> [!TIP]
> **练习题链接**: [Decert Quest: 75782f22](https://decert.me/quests/75782f22-edb8-4e82-9b68-0a4f46fcaadd)

---

## 第十五课

### 15.1 Gas 优化与分析
- 查看当前 `NFTMarket` 的各函数消耗，将测试用例的 Gas Report 记录到 `gas_report_v1.md`
- 尝试优化 `NFTMarket` 合约，尽可能减少 Gas 消耗，并将优化后的测试用例 Gas Report 记录到 `gas_report_v2.md`

> [!TIP]
> **Quest 链接**: [Decert Quest: 6a5ce6d6](https://decert.me/quests/6a5ce6d6-0502-48be-8fe4-e38a0b35df62)

### 15.2 使用内联汇编读写 Slot
- 确定合约中 `Owner` 的 Slot 位置，使用 Solidity 内联汇编（Inline Assembly）直接读取和修改 `Owner`

> [!TIP]
> **Challenge 链接**: [Decert Challenge: 163c68ab](https://decert.me/challenge/163c68ab-8adf-4377-a1c2-b5d0132edc69)

### 15.3 读取私有变量
- 利用对以太坊存储布局（Storage Layout）的理解，读取合约中的私有变量数据

> [!TIP]
> **Quest 链接**: [Decert Quest: b0782759](https://decert.me/quests/b0782759-4995-4bcb-85c2-2af749f0fde9)

---

## 第十六课

### 默克尔树白名单与 Multicall 结合
- 组合使用 `Multicall` (通过 `delegatecall`)、默克尔树（Merkle Tree）、ERC20 Permit 授权，实现一个默克尔树优惠价格（100 Token）购买名单
- **合约名称**：`AirdropMerkleNFTMarket`
- 使用 `Multicall` 同时调用以下 2 个方法：
  - `permitPrePay`：完成预授权
  - `claimNFT()`：验证白名单，如果通过，直接使用 `permitPrePay` 的授权，转入 100 Token，并转出 NFT

> [!TIP]
> **Quest 链接**: [Decert Quest: faa435a5](https://decert.me/quests/faa435a5-f462-4f92-a209-3a7e8fdc4d81)

---

## 第十七课

### 17.1 可升级合约设计与部署
- 编写一个可升级的 ERC721 合约
- 编写一个可升级的 NFT 市场合约：
  - **第一版本**：普通的 NFT 市场合约
  - **第二版本**：加入离线签名上架 NFT 功能（签名内容：`tokenId`，价格），实现用户一次性 `setApprovalForAll` 给 NFT 市场合约后，每次上架只需使用签名即可
- 部署到测试网，并开源到区块链浏览器（在 `README.md` 中备注代理合约及两个逻辑实现合约的地址）

> [!TIP]
> **Quest 链接**: [Decert Quest: ddbdd3c4](https://decert.me/quests/ddbdd3c4-a633-49d7-adf9-34a6292ce3a8)

### 17.2 可升级合约存储布局理解
回答并说明你对以下问题的理解：
1. 编写可升级合约时，如果第一个版本的逻辑实现合约中有一个 `mapping(uint => User) users;`，且 `User` 是一个结构体类型。请问在第二个版本的逻辑实现合约中，可否在 `User` 结构体里添加一个新变量？请说出你的理解。
2. 编写可升级合约时，如果第一个版本的逻辑实现合约中有一个数组 `User[] users;`，且 `User` 是一个结构体类型。请问在第二个版本的逻辑实现合约中，可否在 `User` 结构体里添加一个新变量？请说出你的理解。
3. 编写可升级合约时，如果逻辑实现合约有继承关系，在什么情况下可以在父合约里添加变量？你认为的最佳实践是什么？

> [!TIP]
> **Quest 链接**: [Decert Quest: 8ea21ac0](https://decert.me/quests/8ea21ac0-fc65-414a-8afd-9507c0fa2d90)

---

## 第十八课

### 智能合约安全与重入攻击
- 尝试通过漏洞盗取 `Vault` 合约中的资金

> [!TIP]
> **Quest 链接**: [Decert Quest: b5368265](https://decert.me/quests/b5368265-89b3-4058-8a57-a41bde625f5b)

---

## 第十九课

### 自动化工作流与 Oracles
- 修改 `Bank` 合约：
  - 用户可以通过 `deposit` 进行存款（转账前需先进行 `approve` 授权，该功能之前已实现）
  - **自动化逻辑**：当合约中的存款总额或用户存款超过设定阈值 `x` 时，自动将一半的存款转移到指定的地址（例如 `Owner`）
  - 可以使用 Chainlink Automation、Gelato 等自动化平台实现

> [!TIP]
> **Quest 链接**: [Decert Quest: 072fccb4](https://decert.me/quests/072fccb4-a976-4cf9-933c-c4ef14e0f6eb)
